<nav class="navbar navbar-expand-sm bg-light navbar-light">
    <% if customer_signed_in? %>
    <ul class="navbar-nav">
        <li class="nav-item active nav-link">
            Greetings,
            <%= current_customer.username %>
        </li>
    </ul>
    <ul class="navbar-nav ml-auto">
        <li class="nav-item active">
            <%= button_to "Logout", destroy_customer_session_path, method: :delete, class: "btn btn-link" %>
        </li>
        <li class="nav-item active">
            <%= button_to "Create connection", create_connection_path(current_customer), method: :post, class: "btn btn-link" %>
        </li>
    </ul>
    <% end %>
</nav>